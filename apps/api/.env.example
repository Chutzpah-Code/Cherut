# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Firebase Admin SDK Configuration
# Caminho para o arquivo de credenciais do Firebase
# Baixe este arquivo no Firebase Console > Project Settings > Service Accounts
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-credentials.json

# Firebase Project Info (opcional, caso não use arquivo JSON)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY=your-private-key
# FIREBASE_CLIENT_EMAIL=your-client-email

# Database Configuration (Firebase Firestore é usado automaticamente)
# Não precisa configurar nada adicional para Firestore

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3002,http://localhost:19006

# API Configuration
API_PREFIX=api
API_VERSION=v1
